# Cloudx 0.1.21 Signed Publication

Date: 2026-07-19

## Decision And Boundary

The continuing Roadmap authorization and prior exact publication boundary authorized immutable signed `0.1.21` publication without endpoint activation. Publication did not install or activate either endpoint, move a production selector, stage or activate a CPA candidate, restart a service, mutate credentials or archive state, change Phi, or retire a legacy path.

## Final Preflight

- clean pushed source `f46a0e39887722bd20301270dc3e84d030f8058d`, repository version `0.1.21`
- main CI `29681741098`: success
- non-publishing signing canary `29681791933`: success on the same source
- `v0.1.21` and `release-artifacts/v0.1.21`: absent before publication
- stable before publication: `11edbaf440d0f38958743aea279ddc36ea9320e5`

## Immutable Publication

Annotated tag `v0.1.21` was created and pushed once:

```text
tag object=78a0fea61120ed7780bd8ec7e784ab7bad87a3b0
source commit=f46a0e39887722bd20301270dc3e84d030f8058d
```

Tag workflow `29681834663` completed repository verification, exact tag verification, signing-key load, signed local/cloud build, release-evidence verification, signed-ref publication, and GitHub Release publication.

Published identities:

```text
artifact ref=00148d962a8e90b213b36ef5cb8fcae46a046029
stable ref=2e402391d4a5782af5602738cb2a714248f2a41f
manifest SHA-256=fe6a1086fe1a878a3e73a0485a72c5751b606ce382e4c5da0d4d1599b8fac119
local SHA-256=10370f38b3eb77c609837756a8c0f5292508ddafeace6b8a19b7700d1a83a7c7
cloud SHA-256=0e96deebc121d5a331fc9e9ceb0621ccb330b613940050c95eb48d6da483ccc7
offline bundle SHA-256=b7122c0825d048a9740df72324d58dbff1354c752b74b5518841884a4b98c58f
```

GitHub Release `356304310` is non-draft and non-prerelease and contains exactly seven assets.

## Independent Verification

Fresh immutable artifact/stable clones and authenticated fresh downloads proved:

1. the current signer accepts the manifest and the pre-rotation signer rejects it;
2. the manifest binds version `0.1.21` to exact source `f46a0e3...`;
3. every non-bundle Release asset is byte-identical to the artifact ref;
4. every offline-bundle member is byte-identical to the artifact ref;
5. both self-checks return version `0.1.21`, protocol `1..1`, status `ok`;
6. stable selects this release;
7. isolated local and cloud staging each return `staged`, then `already-staged`;
8. neither isolated root gains `current` or `previous` selectors.

## Runtime Truth And Next Gate

A read-only post-publication snapshot found both real endpoints still selecting signed `0.1.20/0.1.19`. Local CPA remains PID `61859` with 52 established port-`8317` rows, so local CPA activation remains prohibited. Cloud CPA remains PID `1693505` with restart count `0`; importer remains PID `133756` with restart count `0`; the cloud pool has one active credential, zero failure inputs, and no sweep trigger. The observed archive directory contained 46 top-level JSON files at this snapshot. Publication installed no release or CPA policy and performed no service action.

Install signed `0.1.21` cloud first and local second through separate recovery-bounded transactions while retaining signed `0.1.20`. Only afterward may exact `.policy.5` candidates be staged. Cloud `.policy.5` activation remains a distinct externally recoverable CPA restart; local activation additionally requires five consecutive zero-connection samples and must not be attempted while this CPA-backed communication is active.
