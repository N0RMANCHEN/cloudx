# Cloudx 0.1.13 Signed Release And Staging

## Release Identity

Cloudx `0.1.13` carries the two source hardening fixes found during the signed `0.1.12` activation and rollback rehearsal:

- repeated shell-hook installation canonicalizes the Cloudx source block without accumulating `.zshrc` blank lines
- read-only SSH helper commands detach from caller stdin, while import and release-stage commands retain their explicit payload byte pipes

Release identity and hashes are:

- source commit: `d50c7d7b3d1cc390dde7e443fdff05f317a65e54`
- release-key fingerprint: `SHA256:jJAS0rbecTDcU4QD3JjkJFv/5YpFyAOAtvdaf0fY4j0`
- immutable artifact ref: `release-artifacts/v0.1.13`
- artifact ref SHA: `441d174c6ad0b7d30f536e15028d90459cb59004`
- signed stable ref SHA: `ef5520e54a2832133ed1696b356cbfd9a7da4693`
- local artifact SHA-256: `c2fc23d4b742e72dd0ada4bbafa380d3fbe4b3f790376e3484d2759f34afd532`
- cloud artifact SHA-256: `3164ae75489f45ede15faaf8d0274fbcd8eccfc7194f2dac23f4d77ec32d3b96`
- manifest SHA-256: `a4b77470a4cc856775f957e4f816da50ede95400b999564528156a13029d6abb`
- manifest signature SHA-256: `de03719ad277a96e1885b58ba7615f82227623b2b6901432d867ccf68641c80b`
- offline bundle SHA-256: `9a6d9bd261196f46b86ace7e46684fb3f05f785a1336ac474a1edc77507ed4a6`
- stable index SHA-256: `f20151ef3b6534a4fa115b86d30813827a20d173cdb144040cb3b4fd7aa815fc`
- stable signature SHA-256: `faf258568558fc3f679b4cdce51c578e36aff794f0c457bf795ec74030df9b9c`
- packaged shell hook SHA-256: `e76347b0589887000022fb9d1562bd8f722f200a0baec30bdc24ca3011771671`
- private offline archive: `/Users/hirohi/.local/state/cloudx-release/archives/0.1.13`

The private key remained mode `0600` outside Git, release directories, offline bundles, and both endpoints. The private archive and its child directories are mode `0700`; every retained file is mode `0600`.

## Source And Candidate Verification

The exact release source passed architecture validation, 122 tests, and deterministic local/cloud artifact builds. The signed manifest records protocol 1, no automatic activation, no service restart requirement, and the exact source commit above. Both artifacts returned healthy `cloudx.self-check.v1` documents for version `0.1.13`.

The signed stable index selected `refs/heads/release-artifacts/v0.1.13`, matched the manifest digest, and passed verification with the candidate local artifact.

Isolated offline-bundle staging returned:

```text
local  staged -> already-staged
cloud  staged -> already-staged
```

Neither isolated release root created a `current` link. Both staged artifact hashes matched the manifest, and the staged local artifact contained the exact packaged hook hash above.

## Publication Verification

Fresh remote clones of both published refs repeated:

- manifest signature and artifact verification
- stable-index signature and manifest-digest verification
- source commit, version, and immutable artifact-ref verification
- local and cloud self-checks
- exact local, cloud, manifest, signature, index, and stable-signature hashes

The fresh-clone commits matched artifact ref `441d174c6ad0b7d30f536e15028d90459cb59004` and stable ref `ef5520e54a2832133ed1696b356cbfd9a7da4693`.

## Endpoint Staging

The installed signed `0.1.12` updater reported `current=0.1.12`, `available=0.1.13`, and `updateAvailable=true`. Normal dual-endpoint staging from immutable ref `release-artifacts/v0.1.13` returned:

```text
local  staged -> already-staged
cloud  staged -> already-staged
activated false
```

Final endpoint selection remains:

- local current: signed `0.1.12`
- local previous: signed `0.1.11`
- cloud current: signed `0.1.12`
- cloud previous: signed `0.1.11`

The real staged local and cloud artifacts match the signed manifest exactly and both return healthy version `0.1.13` self-checks. The active installed shell hook remains signed `0.1.12`; `.zshrc`, the regenerated account Git shim, and the Cloudx account record retained their accepted hashes.

## Continuity

Staging preserved these active identities and zero restart counts where applicable:

- codex-plus control server: PID `729`
- external local CPA: PID `38189` on `127.0.0.1:8317`
- codex-plus execution parents: PIDs `45303`, `74765`, `79616`, `79871`, and `80474`
- official Codex children: PIDs `45333`, `74770`, `79620`, `79875`, and `80478`
- cloud CLIProxyAPI: PID `977036`, restart count `0`
- old cloud HTTP importer: PID `133756`, restart count `0`

No shell source, account record, regenerated Git shim, service unit, endpoint selector, listener, credential, auth file, or rollback archive was changed by staging.

## Remaining Activation Gate

Signed `0.1.13` is staged but inactive. Cloud-first and local-second activation, installation of the new local artifact behavior, and an exact `0.1.13 -> 0.1.12 -> 0.1.13` rollback rehearsal require a separate explicit operator decision.

The current codex-plus parents and local CPA remain protected. Phi goal-watchdog publication/deployment, HTTP importer retirement, codex-plus package removal, and `legacy_bridge` removal remain independent Roadmap gates.
