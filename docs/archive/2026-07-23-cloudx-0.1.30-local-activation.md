# Cloudx 0.1.30 Local Activation

Date: 2026-07-23

The current workstation truthfully began at signed local `0.1.26`; releases `0.1.27` through `0.1.29` were not selected on this endpoint. After cloud-first acceptance, the direct local updater consumed the signed `0.1.30` stable index, verified the complete release chain, staged beside existing releases, and activated only the local selector.

The secret-free result reported `currentBefore=0.1.26`, `currentAfter=0.1.30`, `stageStatus=staged`, `activationStatus=active`, `verificationScope=complete-release-chain`, `serviceRestarted=false`, `externalCpaManaged=false`, and `officialCodexReplaced=false`.

Protected before/after evidence was byte-identical for:

- native `~/.codex/auth.json` and `config.toml`
- Cloudx shell source
- `com.codexx.cliproxyapi` launcher
- official Codex target binary
- captured official Codex process set

External CPA PID `15946` and its `127.0.0.1:8317` listener remained unchanged. Local selectors became `0.1.30/0.1.26`; current artifact self-check returned `ok`, and the repeated signed-index check reported `up-to-date` without mutation.

In a fresh interactive zsh, `codexx api` selected the external local CPA profile, `command -v codex` resolved official `/opt/homebrew/bin/codex`, and a real official-Codex `gpt-5.6-sol` request returned exact marker `CLOUDX_LOCAL_0_1_30_OK`. Local activation changed no cloud release, gateway key, credential, process, service, or timer.
