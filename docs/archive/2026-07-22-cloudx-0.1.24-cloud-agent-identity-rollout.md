# Cloudx 0.1.24 Cloud Agent Identity Rollout

Date: 2026-07-22

## Scope And Safety Boundary

The operator explicitly authorized the complete signed rollout on both endpoints, including the separately controlled cloud CPA activation and the real local/cloud import checks. This receipt records only aggregate, identity-free evidence. It contains no account address, runtime identifier, private key, token, task value, API key, raw credential, prompt content beyond fixed canary markers, or model response beyond those markers.

Cloudx did not replace or wrap the official `codex` executable. Local CPA remained externally owned and was not restarted. The cloud CPA restart occurred only inside the separately confirmed, rollback-backed policy activation. Ordinary `codexx cloud import` remained a shadow import and did not promote the ten records into the active CPA credential pool.

## Signed Release Identity

Immutable signed `v0.1.24` has these identities:

```text
tag object=f56cf0fe0a2cf02fabb9d7fe9c4ca17aca4b0b01
source commit=fe0eda270866bc1634ef6d50c906caf95d9d3776
artifact ref=f387eb42e396a0e1985979c50e5d2997cf6b6f88
stable ref=bbe4776944858c108fd661afef85e2d00ef5a27b
manifest SHA-256=9b66aea8abab2b6a7c63547a3a33d77a3b41b1721cc9fbb652f655690792f42d
local artifact SHA-256=227d006b9911b4b536eb414e80910e74cb0da1522a13ffdc0879e59f9e2a4f1f
cloud artifact SHA-256=23bdb55f6b8be7b0254f3c7ffbdf3850c7b681b4aabc1274d408935b21506fe0
complete offline bundle SHA-256=24788091ea026857bc39f576db09566f07c047553abe006119351df119c328f7
```

GitHub release workflow `29903990657` completed successfully. Repository verification passed all `535` tests, architecture and privilege checks, legacy/current-N-1 compatibility, release ordering, failure semantics, both zipapp builds, and `git diff --check`. Two independent Linux/amd64 policy6 builds produced the same candidate digest.

## Cloud Endpoint And CPA Activation

The cloud baseline selected signed Cloudx `0.1.23/0.1.22`. CPA policy5 ran as PID `1746294` with restart count `0`; aggregate credential state was active `1`, archive `46`, failures `0`, and shadow `24`. A real official-Codex cloud canary returned `CLOUD_AGENT_IDENTITY_PRE_OK` before service mutation.

The exact signed cloud installer activated Cloudx `0.1.24/0.1.23` from the complete immutable offline bundle. The installer reported `serviceRestarted=false`; handshake and self-check reported product `0.1.24`, protocol `1..1`, and Agent Identity import support. CPA PID, restart count, and aggregate credential state remained unchanged during the Cloudx endpoint install.

The separately confirmed cloud CPA transaction staged and then activated:

```text
runtime=7.2.71-cloudx-policy.6
sha256=d0584d3fddb56e9481d705a74725a77e1841b8525900e40401cb684d54feaf30
size=45359266
rollback backup=1784709569053973954-cloud
capability=codex-agent-identity-v1
```

Activation restarted only `cliproxy.service` behind automatic rollback and selected PID `1860730`, restart count `0`. The configured gateway `/healthz` returned HTTP `200` with `X-Cloudx-Cpa-Capabilities: codex-agent-identity-v1`. Root-owned `cloudx.cloud-cpa-capabilities.v1` binds the exact candidate digest and runtime version. Active `1`, archive `46`, failure `0`, sweep, watcher, and business-policy state remained unchanged. A post-activation official-Codex canary returned `CLOUD_AGENT_IDENTITY_POST_OK`.

## Cloud Import Acceptance

The original failing request had request ID `e86e54fbf3895cc3` and stopped at `missing_token` because the old cloud importer only recognized bearer-style token fields. Signed `0.1.24` instead recognized the bounded Sub2API Agent Identity shape, validated each Ed25519 PKCS#8 private key, discarded imported gateway task/token state, and fingerprinted each runtime/key pair independently.

The authorized cloud sequence produced:

```text
dry-run: status=accepted planned=10 skipped=0
first write: status=accepted written=10 skipped=0
repeat write: status=accepted written=0 skipped=10
final dry-run: status=accepted written=0 skipped=10
```

Aggregate inspection found `10` Agent Identity records, `10` distinct runtime/key pairs, zero token-bearing records, zero imported task state, and private mode on every written record. The shadow inventory changed from `24` to `34`; active CPA capacity remained `1`. This proves accepted and idempotent shadow storage, not live-account promotion or quota health.

## Local Endpoint Acceptance

The workstation baseline selected signed Cloudx `0.1.23/0.1.22`, resolved official `/opt/homebrew/bin/codex`, and kept external CPA PID `15946` listening on `127.0.0.1:8317`. Native Codex auth/config SHA-256 values were:

```text
auth.json=13e0c21c410dd0e7e508d4fe7a1522f8254bcf4a23ac730c230a035095e5edf7
config.toml=9c9290279891d246082fafce270740efb5925c341dbf0dec972d311f93472ad3
```

The pre-install official-Codex-through-local-CPA canary returned `LOCAL_AGENT_IDENTITY_PRE_OK`. The exact signed local installer then activated `0.1.24/0.1.23`, reported `nativeProfileChanged=false`, installed the release-matched shell source, and retained the rollback selector. Official Codex resolution and both native hashes remained identical; CPA PID stayed `15946`. Self-check reported local `0.1.24`, protocol `1..1`, and the post-install canary returned `LOCAL_AGENT_IDENTITY_POST_OK`.

Local dry-run and write both parsed the selected ten-record sample and reported `unchanged=10`, `written=0`, `externalService.managed=false`, and `externalService.restarted=false`. The local aggregate contains `11` Agent Identity records because one independently accepted record predated this ten-record sample; all `11` runtime/key pairs are distinct, mode `0600`, tokenless, and free of imported task state. The existing capability sidecar still binds SHA-256 `85e8a2a051088ce28cabd4a34847eb77a72a36bac90c3f234e7367e61f189f04` to runtime `7.0.2-codexx-fast-service-tier-agent-identity.1` and `codex-agent-identity-v1`.

## Final End-To-End State

Fresh-shell final cloud import preview and write both returned `accepted`, `written=0`, and `skipped=10`. A final official Codex request from signed local `0.1.24` through the Cloudx broker and active cloud policy6 returned `CLOUDX_024_FINAL_OK`.

Both endpoints therefore select signed Cloudx `0.1.24/0.1.23`; cloud policy6 is active with rollback retained, local CPA remained continuously available, and the previously failing ten-record input is accepted and idempotent on both import routes. Promotion from cloud shadow storage into active CPA capacity remains a separate bounded operator transaction and was not authorized or performed by this rollout.
