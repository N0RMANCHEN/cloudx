# Cloudx 0.1.32 Endpoint Activation

Date: 2026-07-23

Cloud-first then local direct upgrades selected signed `0.1.32/0.1.31`. Both reported complete-release-chain verification, `staged -> active`, `serviceRestarted=false`, and `externalCpaManaged=false`; local also reported `officialCodexReplaced=false`.

Cloud self-check binds artifact SHA-256 `3a2b3698b85d7c7951dccf8fc5f4963265b9eb95c1e7000004a8eaa7063ee841`. CPA/gateway PID `1916887`, mihomo PID `277808`, restart counts `0`, one scoped-key rotation transaction, six gateway keys, and one mode-`0600` current credential match remained unchanged.

Local self-check and signed-index status pass. CPA PID `15946` and listener `127.0.0.1:8317`, native auth/config, LaunchAgent, and official Codex hashes remained unchanged. Installation performed no active or shadow account write and no gateway key mutation.
