# Cloudx 0.1.17 Local Activation

Date: 2026-07-18

## Decision And Boundary

The operator supplied the exact confirmation:

```text
INSTALL CLOUDX LOCAL 0.1.17
```

This authorized signed Cloudx `0.1.17` local staging, native-profile reconciliation, shell-source installation, and selector activation only. It did not authorize CPA policy stage or activation, CPA restart, watcher activation, credential/archive mutation, cloud changes, process termination, or legacy removal.

## Preflight

- installer source and pushed `origin/main`: `0cb782a32c1ffb7d300dca44689da4d4ddbb3c6d`
- immutable release source: `0bf7461c9c421b55031c8d17c0951bc8321a0ba9`
- local artifact SHA-256: `b375e12ed9bfede2005bc18efb777135ebf373001bc4bbf311184ad444a52caa`
- manifest SHA-256: `9ea06875527f31cf1ca2577b33afe480a331f25f3e0718935f1d28771ef69aed`
- starting selectors: `current=0.1.13`, `previous=0.1.12`
- signed `0.1.17` release directory absent
- CPA PID and port-8317 listener: `38189`
- six captured Codex/Codex-App PIDs: `15152`, `24464`, `31990`, `37845`, `45333`, `74770`
- official Codex: `/opt/homebrew/bin/codex`
- real official-Codex-through-CPA communication baseline: passed

## Delivery And Activation

The canonical installer fetched the exact signed artifact ref over HTTPS. It returned local stage `staged`, activation `active`, `previousLocal=0.1.13`, a private native-profile backup, and `legacyBackup=null`. It installed the signed shell source and changed no service.

## Acceptance

- local selectors are `current=0.1.17`, `previous=0.1.13`
- artifact and manifest match their published SHA-256 values
- manifest binds version `0.1.17` to source `0bf7461...`
- local self-check reports version `0.1.17`, protocol `1..1`, status `ok`
- `codexx` and `cloud` resolve to the signed `0.1.17` local artifact
- fresh-shell `codexx api` selects the API `CODEX_HOME` and preserves official Codex resolution
- a real post-install official Codex request through the API profile passed
- CPA PID and listener remained `38189`
- all six captured pre-existing Codex PIDs remained alive
- CPA launcher still selects `/Users/hirohi/.local/bin/cli-proxy-api`
- CPA launcher contains neither failure-receipt nor sweep-trigger producer environment

## Next Gate

Both endpoints now run signed Cloudx `0.1.17`. The `.policy.3` candidates remain unstaged and inactive. Cloud policy staging is next and has its own exact confirmation; staging changes no process, listener, launcher, unit, credential, archive, or selector. CPA activation and watcher activation remain later independent gates, and local CPA activation must use the deferred communication-safe worker.
