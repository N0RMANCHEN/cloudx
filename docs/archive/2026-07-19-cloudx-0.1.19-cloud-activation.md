# Cloudx 0.1.19 Cloud Activation

Date: 2026-07-19

## Decision And Boundary

The standing operator authorization accepted the independent exact gate:

```text
INSTALL CLOUDX CLOUD 0.1.19
```

This authorized signed cloud staging and selector activation only. It did not authorize local installation, CPA/importer restart, watcher/unit changes, credential import/archive/restore, policy activation, Phi mutation, or legacy retirement.

## Baseline And Recovery

- immutable source `51c5294ed6dd5b504ef4384e9860c70e2593ae78`
- artifact ref `d4bddf35a1305186ffe568438ceaca6afb5cce61`
- cloud artifact SHA-256 `33b575ec67844f36c2343cbef255cf740a93d288d910cf0405fdab1bb299e430`
- manifest SHA-256 `d6de8428c97ac30371626a6b4f9b41b47da134a5dd3e3900c5d2c90eb3ec996f`
- starting selectors `0.1.18/0.1.17`; target directory absent
- CPA PID `1613475`, importer PID `133756`, both active/running with restart count `0`
- one active credential, 45 reversible archive entries, zero failure receipts, absent sweep trigger
- active watcher path states, systemd bytes, scoped credential/environment bytes, and local CPA PID `61859` captured before mutation

Private transaction `20260719T072339Z-2a6bd2fa` wrote `RECOVERY.txt` before cloning or staging. Its fixed recovery invokes signed `0.1.18` directly to roll the selector back and then runs self-check. Any failed post-activation assertion would have restored the selector and required every service, credential/archive, watcher/unit, and prerequisite digest to equal the baseline.

## Installation And Acceptance

The root transaction cloned immutable tag `v0.1.19` through the existing mihomo HTTP proxy, verified the exact source commit, and invoked that tag's canonical installer with the exact confirmation. The installer fetched only `release-artifacts/v0.1.19`, returned stage `staged`, activation `active`, and `serviceRestarted=false`.

- `/opt/cloudx/current -> /opt/cloudx/releases/0.1.19`
- `/opt/cloudx/previous -> /opt/cloudx/releases/0.1.18`
- installed artifact and manifest match the published SHA-256 values
- signed self-check reports cloud `0.1.19`, protocol `1..1`, status `ok`
- release status reports `0.1.19/0.1.18` and the exact artifact digest
- handshake reports product `0.1.19`
- CPA and importer PIDs, restart counts, active/running states, systemd bytes, watcher states, credential/archive/failure/sweep inventories, and private prerequisites are unchanged
- local remains Cloudx `0.1.18/0.1.17`; CPA remains PID `61859` with 126 established socket rows and ten observed Codex processes
- no service restarted and no credential moved

## Next Gate

Cloud-first ordering is satisfied. Local signed `0.1.19` installation remains a separate transaction with native-profile byte preservation, prebuilt selector recovery, all existing CPA/Codex identities retained, and a real official-Codex-through-CPA canary:

```text
INSTALL CLOUDX LOCAL 0.1.19
```
