# Cloudx 0.1.17 Signed Publication

Date: 2026-07-18

## Decision And Boundary

The operator supplied the exact confirmation:

```text
PUBLISH CLOUDX 0.1.17 SIGNED RELEASE WITHOUT ACTIVATION
```

This authorized immutable tag and signed release publication only. It did not authorize endpoint staging or activation, shell/profile installation, unit changes, service restart, CPA policy staging or activation, watcher activation, credential mutation, archive movement, or legacy removal.

## Final Preflight

- clean worktree and pushed `main`
- repository `VERSION`: `0.1.17`
- local `HEAD` and `origin/main`: `0bf7461c9c421b55031c8d17c0951bc8321a0ba9`
- main CI run `29646011920`: successful on Ubuntu/macOS and Python 3.9/3.12
- non-publishing signing canary `29646089414`: successful on the same source; publication steps skipped
- `v0.1.17` and `release-artifacts/v0.1.17`: absent before publication
- previous stable ref: `bba9f619fc2d3e57cbd1b2808fe97ac58e805aef`

## Immutable Publication

Annotated tag `v0.1.17` was created once and pushed once:

```text
tag object=18fb2fd431dab796eddaa40d5550921d6c38f386
source commit=0bf7461c9c421b55031c8d17c0951bc8321a0ba9
```

Tag workflow `29646167279` completed successfully. Repository verification, exact tag verification, signing-key load, signed local/cloud build, signature/source/stable evidence, signed-ref publication, and GitHub Release publication all passed.

Published identities:

```text
artifact ref=d720979dc46ff5a7b4cb1ba121aca92849e0e09a
stable ref=82a35ff57ae97c4fd655d14ce2bf28c4304cd31b
manifest SHA-256=9ea06875527f31cf1ca2577b33afe480a331f25f3e0718935f1d28771ef69aed
local SHA-256=b375e12ed9bfede2005bc18efb777135ebf373001bc4bbf311184ad444a52caa
cloud SHA-256=bc94accbfcdbff3cf8d33a370e748cf6a25b52546cb15a0b313c5b5274a98480
offline bundle SHA-256=d3c6e726b651e345935821a3605d5fb0eddf7845a1a9deb55c7737009a37a9e9
```

The non-draft, non-prerelease GitHub Release contains exactly seven assets: both artifacts, offline bundle, manifest, signature, signing identity, and allowed signers.

## Independent Verification

Fresh clones of the immutable artifact and stable refs plus fresh downloads of all seven assets proved:

1. the current signer accepts the manifest and the previous signer rejects it;
2. the manifest binds version `0.1.17` to source `0bf7461...`;
3. stable selects `0.1.17` and its manifest digest matches;
4. all non-bundle assets are byte-identical to the immutable artifact ref;
5. every offline-bundle member is byte-identical to the artifact ref;
6. local and cloud self-checks return version `0.1.17`, status `ok`;
7. isolated local and cloud staging each return `staged`, then `already-staged`;
8. neither isolated root gains `current` or `previous` selectors.

## Runtime Truth And Next Gate

After publication, local remained `current=0.1.13`, `previous=0.1.12`, CPA PID `38189`. Cloud remained `current=0.1.16`, `previous=0.1.13`, `cliproxy.service` PID `977036`, restart count `0`; its active auth directory remained empty and its archive retained 45 credential files plus the private manifest. Publication changed no real selector, service, launcher, unit, credential, archive, or policy.

Signed `0.1.17` satisfies the M4B publication gate. Endpoint installation is separately confirmed cloud-first then local. `.policy.3` stage, CPA activation, and dual watcher activation remain later independent gates. Repository development advances to `0.1.18` so later source cannot rebuild the immutable `0.1.17` identity; deployment transactions remain explicitly pinned to active signed `0.1.17`.
