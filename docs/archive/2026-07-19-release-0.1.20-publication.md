# Cloudx 0.1.20 Signed Publication

Date: 2026-07-19

## Decision And Boundary

The standing operator authorization supplied the exact independent gate:

```text
PUBLISH CLOUDX 0.1.20 SIGNED RELEASE WITHOUT ACTIVATION
```

This authorized immutable tag and signed release publication only. It did not install or activate either endpoint, move a production selector, stage or activate a CPA candidate, restart a service, mutate a credential/archive, change Phi, or retire a legacy path.

## Final Preflight

- clean pushed source `eefd8a4aa70d554ac32babe3ff7aa1ae9996e875`, repository version `0.1.20`
- main CI `29679388154`: success
- non-publishing signing canary `29679446529`: success on the same source
- `v0.1.20` and `release-artifacts/v0.1.20`: absent before publication
- stable before publication: `e5f7fafb7539b09f5b3e0f5424999bccd5a06dac`

## Immutable Publication

Annotated tag `v0.1.20` was created and pushed once:

```text
tag object=bd0cfee4bf9073c429a5f210dec2f36723495767
source commit=eefd8a4aa70d554ac32babe3ff7aa1ae9996e875
```

Tag workflow `29679495793` completed repository verification, exact tag verification, signing-key load, signed local/cloud build, release-evidence verification, signed-ref publication, and GitHub Release publication.

Published identities:

```text
artifact ref=d6bce9346d86b2b133057df01ced068ef3eef9b2
stable ref=11edbaf440d0f38958743aea279ddc36ea9320e5
manifest SHA-256=9a9984cf18de1797900ea3710c463abcca20a1958a4809627065f73c3d7e23c7
local SHA-256=bce19e8b27f35bcb95637a5a3afa82c01feb51765763d1b3f34f14dad0e315fd
cloud SHA-256=801a5977362cac6653f18c83efc9bd414126feebc0297cebc35e8c4bd514216e
offline bundle SHA-256=a37dfd8e56e5487fbeb50efc1faad682e14a70ddac42bab9efb6050803915e79
```

GitHub Release `356290726` is non-draft and non-prerelease and contains exactly seven assets.

## Independent Verification

Fresh immutable artifact/stable clones and authenticated fresh downloads proved:

1. the current signer accepts the manifest and the pre-rotation signer rejects it;
2. the manifest binds version `0.1.20` to exact source `eefd8a4...`;
3. every non-bundle Release asset is byte-identical to the artifact ref;
4. every offline-bundle member is byte-identical to the artifact ref;
5. both self-checks return version `0.1.20`, protocol `1..1`, status `ok`;
6. stable selects this release;
7. isolated local and cloud staging each return `staged`, then `already-staged`;
8. neither isolated root gains `current` or `previous` selectors.

## Runtime Truth And Next Gate

After publication, local and cloud still select signed `0.1.19/0.1.18`. Local CPA remains PID `61859`; cloud CPA remains PID `1613475`, restart count `0`; importer remains PID `133756`, restart count `0`; the cloud active pool retains one credential, the archive retains 45 entries, and failure/trigger inputs are empty.

Publication changed no real release directory, selector, shell hook, profile, launcher, unit, process, listener, credential, archive, watcher, CPA policy, or Phi state. Install cloud then local through separate exact gates with signed `0.1.19` retained. Only afterward may `.policy.4` be staged, and its activation remains a separate external CPA restart boundary.
