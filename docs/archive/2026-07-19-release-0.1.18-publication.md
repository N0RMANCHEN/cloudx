# Cloudx 0.1.18 Signed Publication

Date: 2026-07-19

## Decision And Boundary

The operator supplied the exact confirmation:

```text
PUBLISH CLOUDX 0.1.18 SIGNED RELEASE WITHOUT ACTIVATION
```

This authorized immutable tag and signed release publication only. It did not authorize endpoint staging or activation, shell/profile installation, selector movement, unit changes, service restart, CPA policy or watcher activation, credential mutation, archive movement, Phi mutation, or legacy removal.

## Final Preflight

- clean worktree and pushed `main`
- repository `VERSION`: `0.1.18`
- local `HEAD` and `origin/main`: `f94c926daa929d8d9801722013408eb9ebb7e90a`
- main CI run `29653018964`: successful on the complete matrix
- non-publishing signing canary `29653206898`: successful on the same source; both publication steps skipped
- `v0.1.18` and `release-artifacts/v0.1.18`: absent before publication
- previous stable ref: `82a35ff57ae97c4fd655d14ce2bf28c4304cd31b`

## Immutable Publication

Annotated tag `v0.1.18` was created once and pushed once:

```text
tag object=0798a6c4bc3d4004fec56000306601a18fd1b857
source commit=f94c926daa929d8d9801722013408eb9ebb7e90a
```

Tag workflow `29653241622` completed successfully. Repository verification, exact tag verification, signing-key load, signed local/cloud build, signature/source/stable evidence, signed-ref publication, and GitHub Release publication all passed.

Published identities:

```text
artifact ref=5e345ed8cbf0373ac9e6214e45358187a524ea5d
stable ref=d3275fd150bceaa50c216c13a99969b58409992d
manifest SHA-256=b5ab79e932f215730452dd6aa8afabe629a23118a489ec5c03054d5e0856ee23
local SHA-256=56881ca376857652cefaecc5159f8818edf765702f3d574c3b55f37972edde1f
cloud SHA-256=1cba50617522634c09584d7cc9d1a800df056256c0201fd325cb54afa8ca1426
offline bundle SHA-256=a4ae5d73b1ec6a25f64e9a4f86467a3f7be07455ef6d30f510a9286a87bc6738
```

GitHub Release `356151215` is non-draft and non-prerelease and contains exactly seven assets: both artifacts, offline bundle, manifest, signature, signing identity, and allowed signers.

## Independent Verification

Fresh clones of the immutable artifact and stable refs plus fresh downloads of all seven assets proved:

1. the current signer accepts the manifest and the previous signer rejects it;
2. the manifest binds version `0.1.18` to source `f94c926...` and declares no automatic activation or required service restart;
3. stable selects `refs/heads/release-artifacts/v0.1.18` and its manifest digest matches;
4. all non-bundle Release assets are byte-identical to the immutable artifact ref;
5. every offline-bundle member is byte-identical to the artifact ref;
6. local and cloud self-checks return version `0.1.18`, protocol `1..1`, status `ok`;
7. isolated local and cloud staging each return `staged`, then `already-staged`;
8. neither isolated root gains `current` or `previous` selectors.

## Runtime Truth And Next Gate

After publication, local still selects Cloudx `0.1.17/0.1.13`. The external CPA remains healthy as PID `61859`, listening on `127.0.0.1:8317`; launchd still selects `/Users/hirohi/.local/bin/cli-proxy-api`, reports that it has never exited, and `/healthz` returns `ok`.

Cloud still selects Cloudx `0.1.17/0.1.16`. `cliproxy.service` remains active/running as PID `1613475` with restart count `0`, selects the already active `7.2.71-cloudx-policy.3` binary, returns healthy on its declared Tailscale listener, and retains zero active auth JSON files.

Publication and verification changed no real release directory, selector, shell hook, profile, launcher, unit, process, listener, credential, archive, watcher, CPA policy, or Phi state.

Signed `0.1.18` satisfies the local CPA recovery publication prerequisite. Real endpoint installation remains separately confirmed in cloud-first then local order. Local `.policy.3` activation remains a later distinct confirmation after signed `0.1.18` is active locally and the prepared recovery bundle plus repeated zero-connection gate pass. Repository development advances to `0.1.19` so later source cannot rebuild the immutable `0.1.18` identity.
