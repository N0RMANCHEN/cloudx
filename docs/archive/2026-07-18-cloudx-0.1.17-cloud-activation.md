# Cloudx 0.1.17 Cloud Activation

Date: 2026-07-18

## Decision And Boundary

The operator supplied the exact confirmation:

```text
INSTALL CLOUDX CLOUD 0.1.17
```

This authorized signed Cloudx `0.1.17` staging and selector activation on the cloud endpoint only. It did not authorize local activation, CPA policy staging or activation, CPA restart, watcher or unit installation, credential import, archive mutation, importer change, or legacy removal.

## Preflight

- immutable source: `0bf7461c9c421b55031c8d17c0951bc8321a0ba9`
- immutable artifact ref: `d720979dc46ff5a7b4cb1ba121aca92849e0e09a`
- manifest SHA-256: `9ea06875527f31cf1ca2577b33afe480a331f25f3e0718935f1d28771ef69aed`
- cloud artifact SHA-256: `bc94accbfcdbff3cf8d33a370e748cf6a25b52546cb15a0b313c5b5274a98480`
- starting selectors: `current=0.1.16`, `previous=0.1.13`
- starting CPA: PID `977036`, restart count `0`, active/running
- starting importer: PID `133756`, restart count `0`, active/running
- zero active auth files and 45 entries in the reversible archive manifest
- `0.1.17` release directory absent
- required client credential and shadow environment were regular, non-symlink, private files
- the cloud host could read the exact public artifact ref directly; a complete local mode-`0600` bundle was prepared but never uploaded or used

## Delivery And Activation

The canonical installer ran from a temporary clone of immutable tag `v0.1.17`, verified its source commit, fetched the exact signed artifact ref over HTTPS, staged it, and atomically activated the selector. It returned:

- stage status: `staged`
- activation status: `active`
- current version: `0.1.17`
- previous version: `0.1.16`
- service restarted: `false`

Root-owned Python cache files initially prevented ordinary-user cleanup of the temporary source clone. The exact temporary directory was then removed with `sudo`; no release or runtime path was included in that cleanup.

## Acceptance

- `/opt/cloudx/current -> /opt/cloudx/releases/0.1.17`
- `/opt/cloudx/previous -> /opt/cloudx/releases/0.1.16`
- current cloud artifact and manifest match their published SHA-256 values
- manifest binds version `0.1.17` to source `0bf7461...`
- self-check reports cloud `0.1.17`, protocol `1..1`, status `ok`
- release status reports `current=0.1.17`, `previous=0.1.16`, status `active`
- handshake reports `productVersion=0.1.17` and gateway version `7.2.71`
- CPA remained PID `977036`, restart count `0`, active/running
- importer remained PID `133756`, restart count `0`, active/running
- active auth remained empty; archive manifest retained 45 entries; no symlink appeared
- inspected CPA health, policy drop-in, failure watcher, and sweep watcher unit bytes were unchanged
- a natural CPA-health run exited `0` with zero accounts, `probe_gate=no_accounts`, and `probe_concurrency=0`

## Next Gate

Local signed `0.1.17` installation remains separately confirmed and does not restart CPA. Both `.policy.3` candidates remain unstaged and inactive. The existing cloud CPA-health unit still invokes the pre-watcher command because endpoint installation changes no unit; before usable credentials are imported, the separate watcher transaction must install the signed trigger-aware health service/timer and both failure/sweep path pairs with rollback protection.
