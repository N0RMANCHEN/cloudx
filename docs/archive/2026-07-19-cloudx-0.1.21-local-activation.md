# Cloudx 0.1.21 Local Activation

Date: 2026-07-19

## Decision And Boundary

Cloud `0.1.21` had already passed its independent recovery-bounded transaction. The continuing Roadmap authorization accepted:

```text
INSTALL CLOUDX LOCAL 0.1.21
```

This authorized signed local staging, selector/entrypoint activation, and shell-source reconciliation only. It did not authorize CPA restart, policy/watcher activation, credential mutation, cloud mutation, Phi mutation, or legacy retirement.

## Baseline And Recovery

- starting selectors `0.1.20/0.1.19`; target release absent
- immutable source `f46a0e39887722bd20301270dc3e84d030f8058d`
- local artifact SHA-256 `10370f38b3eb77c609837756a8c0f5292508ddafeace6b8a19b7700d1a83a7c7`
- manifest SHA-256 `fe6a1086fe1a878a3e73a0485a72c5751b606ce382e4c5da0d4d1599b8fac119`
- exact native auth/config, `.zshrc`, signed shell source, CPA binary/config/LaunchAgent, 37-file JSON aggregate, CPA PID `61859`, listener identity, and six existing Codex PIDs captured
- private recovery job `20260719T095649Z-5e63f3c8` prepared before staging
- recovery snapshots and state are mode `0600`; transaction/apply/recovery tools are mode `0700`
- recovery `--check` verified bundle/tool/snapshot digests, signed `0.1.20/0.1.19`, exact selectors, original CPA PID/health, and a real official-Codex-through-CPA canary; the transaction then recaptured the final post-check baseline before installation
- the apply wrapper automatically invokes the same manual recovery command on any installer, acceptance, or post-canary failure

Manual recovery remains:

```text
/Users/hirohi/.local/state/cloudx/local-install-jobs/20260719T095649Z-5e63f3c8/recover.sh 'RECOVER CLOUDX LOCAL 0.1.21 TO 0.1.20'
```

It restores selectors to `0.1.20/0.1.19`, restores exact native/shell snapshots, requires CPA PID `61859` and health, and runs a real official-Codex-through-CPA recovery canary without stopping or restarting CPA or terminating a Codex process.

## Installation And Acceptance

The transaction cloned immutable `v0.1.21` from the same complete deploy bundle used by cloud, verified tag/source identity and the non-authorizing installer plan, then invoked the tagged canonical installer. It returned stage `staged`, activation `active`, `nativeProfileChanged=false`, and no legacy backup.

- `current=/Users/hirohi/.local/lib/cloudx/releases/0.1.21`
- `previous=/Users/hirohi/.local/lib/cloudx/releases/0.1.20`
- artifact and manifest match the published SHA-256 values
- self-check reports local `0.1.21`, protocol `1..1`, status `ok`
- `codexx`, `cloud`, and `cloudx-update` point through signed `current`
- fresh-shell `codexx api` resolves mode `api`, `/Users/hirohi/.codex-accounts/api/.codex`, and official `/opt/homebrew/bin/codex`
- native auth/config, `.zshrc`, shell source, CPA binary/config/LaunchAgent, and the 37-file JSON aggregate match the final baseline exactly
- CPA and listener remain PID `61859`; all six captured Codex PIDs survived
- real official-Codex-through-CPA acceptance traffic passed after activation
- cloud remains `0.1.21/0.1.20` with CPA PID `1693505`, importer PID `133756`, and restart counts `0`
- no service restarted

## Next Gate

Both endpoints now run signed Cloudx `0.1.21` with signed `0.1.20` retained. Exact `.policy.5` staging is process-neutral and may proceed cloud then local through separate confirmations. Activation remains separate; local activation is prohibited while any established CPA connection remains, and no Codex process may be terminated to manufacture quiescence.
