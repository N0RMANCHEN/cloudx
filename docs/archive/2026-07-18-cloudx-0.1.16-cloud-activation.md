# Cloudx 0.1.16 Cloud Activation

Date: 2026-07-18

## Decision And Boundary

The operator supplied the exact confirmation:

```text
INSTALL CLOUDX CLOUD 0.1.16
```

This authorized signed Cloudx `0.1.16` staging and selector activation on the cloud endpoint only. It did not authorize local Cloudx activation, CPA activation, CPA restart, account archive, credential mutation, unit replacement, importer change, or legacy removal.

## Preflight

- published source: `ec77369a990418f2a990874d1d7bd4b9d2c7fe04`
- immutable artifact ref: `9513ff87b3b2e45d2b3609f0746248a7422d34b2`
- manifest SHA-256: `9ac48648b9e00bc0fcdbc33517cb3f97bb545211829626e6423281549f7b4fee`
- cloud artifact SHA-256: `b6257a5bbe128f159019ea0592be6eb3f3f5e38d5e109bdb293af06230ed8cfe`
- starting selectors: `current=0.1.13`, `previous=0.1.12`
- starting CPA: PID `977036`, restart count `0`, active/running, baseline `/usr/local/bin/cli-proxy-api`
- starting Cloudx self-check: cloud `0.1.13`, protocol `1..1`, status `ok`
- required credential/environment files: regular, private, and owned by their declared identities

## Delivery And Activation

The first canonical installer invocation attempted to fetch `release-artifacts/v0.1.16` from public GitHub on the cloud host and failed before staging. Immediate checks proved selectors, release directories, CPA PID/restart count, and the active `0.1.13` self-check were unchanged.

A fresh independently verified clone of the immutable artifact ref was converted into a complete Git bundle. The bundle had SHA-256 `b77928f9d7e7e88cfa7871b660c79c077e4da16b2bcc3879c8669395c75f4540` on both local and cloud hosts and exposed only:

```text
9513ff87b3b2e45d2b3609f0746248a7422d34b2 refs/heads/release-artifacts/v0.1.16
```

The same canonical installer then consumed that bundle with the original exact confirmation. It returned:

- stage status: `staged`
- activation status: `active`
- current version: `0.1.16`
- previous version: `0.1.13`
- service restarted: `false`

## Acceptance

- `/opt/cloudx/current -> /opt/cloudx/releases/0.1.16`
- `/opt/cloudx/previous -> /opt/cloudx/releases/0.1.13`
- current artifact SHA-256 matches `b6257a5bbe128f159019ea0592be6eb3f3f5e38d5e109bdb293af06230ed8cfe`
- current manifest SHA-256 matches `9ac48648b9e00bc0fcdbc33517cb3f97bb545211829626e6423281549f7b4fee`
- cloud self-check reports version `0.1.16`, status `ok`
- release status reports `current=0.1.16`, `previous=0.1.13`, status `active`
- normal SSH `cloudx-remote handshake` reports `productVersion=0.1.16` and gateway version `7.2.71`
- the existing CPA-health timer naturally invoked the `/opt/cloudx/current` command after cutover and exited `0`
- that aggregate-only run truthfully reported state `probe_error`, observed 45 total auth records, archived zero, and produced no pending archive candidate
- top-level inventory remained 45 regular auth files, zero archive files, zero symlinks; the failure-receipt directory remains absent until CPA policy activation
- `cliproxy.service` remains PID `977036`, restart count `0`, active/running, and selected the baseline binary

## Next Gate

Local Cloudx `0.1.16` activation remains separately confirmed. Cloud and local CPA candidates remain staged but inactive. Cloud CPA activation must later add only the declared failure-directory drop-ins, restart only `cliproxy.service`, pass health/policy canaries, and retain its original binary plus rollback snapshot.
