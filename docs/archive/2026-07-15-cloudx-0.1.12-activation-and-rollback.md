# Cloudx 0.1.12 Activation And Rollback

## Scope

This batch completes the `/Users/hirohi` workstation build prerequisite that remained after signed `0.1.12` publication and staging. The release was activated cloud-first and local-second, exercised through an exact N-1 rollback round trip, and left active on both endpoints without terminating or replacing the `codexx use api` communication path.

No CLIProxyAPI, importer, Phi, Tailscale, SSH, mihomo, launchd, or systemd service was restarted. The existing codex-plus control and execution processes were treated as protected external state throughout the transaction.

## Accepted Baseline

Before activation:

- local current: signed `0.1.11`, artifact SHA-256 `0a52376e8b222cfe7b8a97db00573556cc3e44ff95ce387ea962640e27d9258b`
- local previous: signed `0.1.10`
- cloud current: signed `0.1.11`, artifact SHA-256 `00b388c9f9ab09d597777b721b805a7e827e1cf3fc91a37db0b04551214c51ba`
- cloud previous: signed `0.1.10`
- staged local `0.1.12`: SHA-256 `10065be877ab788ca3a9b7cd1ebb5013369989d98007b3dea31472d0035fec28`
- staged cloud `0.1.12`: SHA-256 `e6ecb020465d2c681ecee4580b5f55258f1802b7247792940363460e8107d6ba`
- installed `0.1.11` hook: SHA-256 `dbfa8d60c92cdfd3d3a7422ecedef7dd545b1e53a976ca089ddd17963c1c770b`
- `.zshrc`: SHA-256 `a93057dc43c7e51dd5f3717c61c0399efc82b28b5635ecb1778ffa44657773a2`
- regenerated API-account Git shim: SHA-256 `11233682717b6b2fad7323f4c395f06073b0175d83ce99723ac684b0dd55594e`
- Cloudx account record: SHA-256 `3de0fac1daecd5b826275d01dd2d861881e12333ce59779dc0cf013a18d58166`

The private `codexx-legacy api status` recovery command reported the local CPA binary, configuration, account profile, model list, launchd unit, and refresh job healthy. Formal remote handshake and health returned protocol 1, healthy gateway state, ready import state, and product `0.1.11`. A sanitized SSH import dry-run was accepted with one prospective write and no errors.

Protected local identities were:

- codex-plus control PID `729`
- external local CPA PID `38189`, listening only on `127.0.0.1:8317`
- codex-plus execution parents `45303`, `74765`, `79616`, `79871`, and `80474`
- official Codex children `45333`, `74770`, `79620`, `79875`, and `80478`

The legacy local port `18317` was not bound on this workstation.

## Ordered Activation

The installed signed updater performed only endpoint-specific confirmed changes:

1. `cloudx-update apply 0.1.12 --confirm 0.1.12 --cloud-only`
2. verify remote release status, handshake, formal health, and SSH import dry-run
3. `cloudx-update apply 0.1.12 --confirm 0.1.12 --local-only --install-shell-hook`
4. verify local selectors, hook hash, legacy-environment reload, account selection, broker, and model paths

Cloud release status reported current `0.1.12`, previous `0.1.11`, and the exact signed cloud artifact hash. The installed local hook reported SHA-256 `e76347b0589887000022fb9d1562bd8f722f200a0baec30bdc24ca3011771671`, with local current `0.1.12` and previous `0.1.11`.

An isolated shell that inherited the live codex-plus API `HOME` and account-local `PATH` sourced the installed hook twice and observed:

- restored `HOME=/Users/hirohi`
- zero account `.local/bin` entries
- preserved account Codex temporary path
- Git `/opt/homebrew/bin/git`, version `2.46.0`
- Codex `/opt/homebrew/bin/codex`, version `0.144.4`
- `codexx use api` selected `/Users/hirohi/.codex-accounts/api/.codex`
- named `soul0` selection retained real home
- `codexx exit` cleared Cloudx mode and `CODEX_HOME`

Complete ephemeral official-Codex requests returned exactly:

```text
CLOUDX_LOCAL_012_API_OK
CLOUDX_LOCAL_012_CLOUD_OK
```

The Cloudx broker check returned gateway HTTP `200`. After the cloud model request, broker PID `5097` and SSH PID `5098` remained healthy with zero leases.

## N-1 Rehearsal

The rollback covered both selectors and the release-matched local hook:

1. local rollback `0.1.12 -> 0.1.11`
2. local same-version apply of `0.1.11` with `--install-shell-hook`
3. verify local current `0.1.11`, previous `0.1.12`, exact `0.1.11` hook hash, official Codex resolution, and API selection in a clean shell
4. cloud rollback `0.1.12 -> 0.1.11`
5. verify matched `0.1.11` handshake, health, SSH import dry-run, and service continuity
6. cloud reactivation `0.1.11 -> 0.1.12`
7. local reactivation `0.1.11 -> 0.1.12` with the signed `0.1.12` hook

The final API request after reactivation returned exactly `CLOUDX_LOCAL_012_FINAL_OK`. Final endpoint state is:

- local current: signed `0.1.12`
- local previous: signed `0.1.11`
- cloud current: signed `0.1.12`
- cloud previous: signed `0.1.11`

Final formal health reported schema `cloudx.health.v1`, producer `0.1.12`, protocol 1, fresh state, healthy gateway, ready import, 15 total accounts, 13 available, 2 limited, and 0 unavailable. The sanitized import dry-run remained accepted.

## Continuity

All protected local PIDs and their original parent/start identities remained present after activation, rollback, reactivation, and canaries. The local CPA retained PID `38189` and listener `127.0.0.1:8317`.

Cloud external services retained:

- `cliproxy.service`: PID `977036`, restart count `0`, original active timestamp
- `codex-import.service`: PID `133756`, restart count `0`, original active timestamp

The regenerated Git shim and Cloudx account record retained their exact baseline hashes. The private recovery command continued to report the local CPA and model endpoint healthy.

Repeated signed hook installation exposed whitespace-only `.zshrc` drift: each installation added one blank line before the existing Cloudx block. The three rehearsal-added lines were removed after the final activation, restoring `.zshrc` byte-for-byte to its baseline hash while leaving the signed `0.1.12` hook active.

## Source 0.1.13 Follow-Up

The rehearsal exposed two source-only defects; immutable signed `0.1.12` was not modified:

- hook installation now removes trailing blank lines before appending the single Cloudx source block, making repeated install/rollback whitespace-idempotent
- read-only SSH helper commands now use detached stdin, while import and release-stage operations still pipe their explicit payload bytes

Focused regressions pass. A built `0.1.13` local artifact successfully selected cloud mode from a heredoc without consuming the remaining commands, then released the mode normally. Full `./verify.sh` passed architecture validation and 122 tests. A subsequent deterministic `./build.sh` produced:

- local SHA-256: `c2fc23d4b742e72dd0ada4bbafa380d3fbe4b3f790376e3484d2759f34afd532`
- cloud SHA-256: `3164ae75489f45ede15faaf8d0274fbcd8eccfc7194f2dac23f4d77ec32d3b96`

These `0.1.13` artifacts are development builds only. They are not signed, published, staged, or installed.

## Remaining Gates

This completes the current workstation build and signed rollback prerequisite. It does not authorize codex-plus shell/package deletion because the protected codex-plus parents remain active and the regenerated account shim remains part of their recovery path.

The Phi goal-watchdog candidate remains isolated, uncommitted, and undeployed. The old HTTP importer remains enabled and active pending Phi-owned consumer migration and a separate stop/rollback decision. `legacy_bridge`, recovery archives, the local CPA, and all legacy service evidence remain available.
