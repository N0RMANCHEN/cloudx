# Cloudx 0.1.15 Signed Publication

Date: 2026-07-17

## Decision And Boundary

The operator supplied the exact confirmation:

```text
PUBLISH CLOUDX 0.1.15 SIGNED RELEASE WITHOUT ACTIVATION
```

This authorized immutable tag and signed release publication only. It did not authorize real endpoint staging or activation, shell/profile installation, unit installation, service restart, credential mutation, HTTP importer stop, legacy bridge cutover, or legacy removal.

## Final Preflight

- worktree: clean
- repository `VERSION`: `0.1.15`
- local `HEAD` and `origin/main`: `9ffa3208f39053c2b3af1136a530ce98eac7ad41`
- final verification-only workflow run: `29579445629`, successful on the same HEAD
- GitHub `release` environment signing secret: present
- signer fingerprint: `SHA256:oEhvhqj9U4wM8zLz8w43A/fvMN+BRNXO1k5/3eVPh9o`
- `v0.1.15`: absent before publication
- `release-artifacts/v0.1.15`: absent before publication
- previous stable ref: `ef5520e54a2832133ed1696b356cbfd9a7da4693`

The final canary completed verification, key loading, signed build, and signed release evidence checks; both publication steps were skipped and release refs remained unchanged.

## Immutable Publication

Annotated tag `v0.1.15` was created in the same format as prior releases and pushed once. It resolves as:

```text
tag object=5feff710504403b7dc3167067ae560174320b0e6
source commit=9ffa3208f39053c2b3af1136a530ce98eac7ad41
```

Tag-triggered workflow `29579921061` ran from `2026-07-17T12:21:05Z` through `2026-07-17T12:21:44Z` and concluded successfully. The release job passed:

- repository `./verify.sh`
- exact release-tag verification
- signing-key load
- signed local/cloud release build
- manifest/source/signature/stable evidence verification
- immutable signed-ref publication
- GitHub Release asset publication

The published identities are:

```text
artifact ref commit=332cb865a97d654efca4b4321b90cdc140e57e64
stable ref commit=78fe78303f9d57c592b103e11c0fdca1c373b37c
source commit=9ffa3208f39053c2b3af1136a530ce98eac7ad41
signer fingerprint=SHA256:oEhvhqj9U4wM8zLz8w43A/fvMN+BRNXO1k5/3eVPh9o
manifest SHA-256=3d1f9747cefab855725d105f584e938a27bc93baf488598b746418978547595a
```

The GitHub Release is neither draft nor prerelease and contains exactly:

- `SIGNING_IDENTITY`
- `allowed_signers`
- `cloudx-local-0.1.15.pyz`
- `cloudx-cloud-0.1.15.pyz`
- `cloudx-0.1.15-offline.tar.gz`
- `manifest.json`
- `manifest.json.sig`

Artifact hashes are:

```text
cloudx-local-0.1.15.pyz  9d9d400ce16630eae7e9dddcf17e837ef05d3315a709ff4bbc12abcf10308e97
cloudx-cloud-0.1.15.pyz  7e838757727e90b11029d85966525a621f629e5f400fa113abc6168790878b71
cloudx-0.1.15-offline.tar.gz  5f523fec6c1d53b4b3ee8c1e2a252daf552888eb0e3af39a97a13cd43534b89a
```

## Independent Fresh Verification

Fresh shallow clones of `release-artifacts/v0.1.15` and `release/stable`, plus a fresh GitHub Release asset download, independently proved:

1. the replacement root accepts the manifest signature;
2. the previous root rejects the same manifest signature;
3. stable selects `refs/heads/release-artifacts/v0.1.15` and its manifest digest matches;
4. every non-bundle GitHub asset is byte-identical to the immutable artifact ref;
5. both downloaded artifacts report `cloudx.self-check.v1`, version `0.1.15`, status `ok`;
6. the downloaded local artifact accepted stable and staged the offline bundle as `staged`, then `already-staged`;
7. the downloaded cloud artifact staged the same bundle as `staged`, then `already-staged`;
8. both isolated roots remained without `current` or `previous` selectors.

## Runtime Truth And Next Gate

No real local or cloud release directory, selector, shell hook, profile, account, external CPA process, gateway/importer process, listener, systemd unit, Phi credential, or health publisher changed.

The legacy health bridge's publication blocker is now satisfied. Its remaining blockers are fixed-unit installation and independent production rollback/cutover acceptance. Because installed older clients embed the previous trust root, real `0.1.15` stage-only recovery must use the separately confirmed repository root or exact candidate-verified offline path; activation remains a later explicit decision.

Repository development advances immediately to `0.1.16` so no later source change can produce a second artifact under the immutable `0.1.15` identity.

Post-publication closeout updated all three source version declarations to `0.1.16`, bound bridge evidence to the exact `0.1.15` tag/source/artifact/manifest/signer identities, and marked only `signedArtifactPublished=true`. Full `./verify.sh` then passed architecture validation, 331 tests, truthful two-blocker bridge status, and healthy local/cloud `0.1.16` development builds.
