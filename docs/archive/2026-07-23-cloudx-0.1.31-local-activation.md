# Cloudx 0.1.31 Local Activation

Date: 2026-07-23

After cloud-first acceptance, the direct local updater consumed the signed `0.1.31` stable index, verified the complete release chain, staged beside existing releases, and activated only the local selector. The secret-free result reported `currentBefore=0.1.30`, `currentAfter=0.1.31`, `stageStatus=staged`, `activationStatus=active`, `verificationScope=complete-release-chain`, `serviceRestarted=false`, `externalCpaManaged=false`, and `officialCodexReplaced=false`.

Protected before/after SHA-256 evidence was byte-identical for:

- native `~/.codex/auth.json` and `config.toml`
- `.zshrc` and the Cloudx shell source
- `com.codexx.cliproxyapi` launcher
- official `/opt/homebrew/bin/codex`

External CPA PID `15946` and its `127.0.0.1:8317` listener remained unchanged. Two of nine captured Codex processes exited naturally during the observation interval; every remaining captured PID survived, and the updater neither manages nor terminates Codex processes. Local selectors became `0.1.31/0.1.30`; current artifact self-check returned `ok`, and the repeated signed-index check reported `up-to-date` without mutation.

In a fresh interactive zsh, `codexx api` selected the external local CPA profile, `command -v codex` resolved official `/opt/homebrew/bin/codex`, and a real official-Codex `gpt-5.6-sol` request returned exact marker `CLOUDX_LOCAL_0_1_31_OK`. Local activation changed no cloud selector, gateway key, credential, service, or timer.
