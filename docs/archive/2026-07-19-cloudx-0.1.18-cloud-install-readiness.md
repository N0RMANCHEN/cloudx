# Cloudx 0.1.18 Cloud Install Readiness

Date: 2026-07-19

## Boundary

No `INSTALL CLOUDX CLOUD 0.1.18` confirmation had been supplied during this audit. The transaction therefore remained read only apart from an automatically removed remote temporary clone. It did not create `/opt/cloudx/releases/0.1.18`, move `current` or `previous`, install a unit, restart a service, change a credential/archive, activate a watcher or CPA policy, or mutate Phi.

## Immutable Candidate

The exact accepted release identities remain:

```text
tag source=f94c926daa929d8d9801722013408eb9ebb7e90a
artifact ref=5e345ed8cbf0373ac9e6214e45358187a524ea5d
manifest SHA-256=b5ab79e932f215730452dd6aa8afabe629a23118a489ec5c03054d5e0856ee23
cloud artifact SHA-256=1cba50617522634c09584d7cc9d1a800df056256c0201fd325cb54afa8ca1426
```

The canonical installer was loaded from immutable tag `v0.1.18`, resolved to that exact source, and returned the non-authorizing plan:

```text
confirmation=INSTALL CLOUDX CLOUD 0.1.18
releaseSource=release-artifacts/v0.1.18
actions=stage signed artifact; activate helper/current; no service restart
```

## Cloud Baseline

Read-only inspection established:

- `current=/opt/cloudx/releases/0.1.17`
- `previous=/opt/cloudx/releases/0.1.16`
- `/opt/cloudx/releases/0.1.18` is absent
- signed current self-check and release status are healthy
- `cliproxy.service` remains PID `1613475`, active/running, result `success`, restart count `0`
- `codex-import.service` remains PID `133756`, active/running, result `success`, restart count `0`
- the declared Tailscale CPA listener returns `{"status":"ok"}`
- active auth JSON count is `0`; the private reversible archive manifest retains `45` entries
- failure receipt and aggregate sweep trigger counts are both `0`
- `/etc/cloudx/client-credential` is a regular `cloudx:cloudx` mode-`0600` file
- `/etc/cloudx/cloudx-shadow.env` is a regular `root:cloudx` mode-`0640` file
- more than 16 GiB is available under the `/opt/cloudx` filesystem
- cloud failure and sweep path units remain absent/inactive

The local communication path remained independent and healthy throughout: Cloudx stayed `0.1.17/0.1.13`, local CPA stayed PID `61859` on its baseline launchd program, `/healthz` returned `ok`, and launchd continued to report that the process had never exited.

## Fetch Path

Direct cloud-host TCP access to `github.com:443` timed out, so the default unaided installer fetch is not accepted for the maintenance action. The external mihomo dependency remains active as PID `277808`; its loopback HTTP and SOCKS listeners both returned GitHub HTTP `200`.

An automatically removed temporary clone then used explicit `HTTP_PROXY`/`HTTPS_PROXY=http://127.0.0.1:7890` plus Git HTTP/1.1 to fetch `release-artifacts/v0.1.18`. On the cloud host it independently matched the artifact ref, source commit, manifest digest, and cloud artifact digest above, accepted the release signature, and returned cloud self-check version `0.1.18`, protocol `1..1`, status `ok`. No temporary candidate state was retained.

The approved install transaction must therefore pass the proxy environment explicitly to the immutable installer. A complete digest-verified offline Git bundle remains the non-network fallback; neither path authorizes changing mihomo, system proxy configuration, Git global configuration, or the release source identity.

## Acceptance And Rollback Contract

After a future exact confirmation, acceptance must prove:

1. stage status is `staged` or `already-staged`, activation status is `active`, `current=0.1.18`, and `previous=0.1.17`;
2. current manifest/artifact/source identities match the immutable values above;
3. self-check, release status, and handshake accept version `0.1.18`;
4. CPA and importer PIDs, restart counts, health, credentials, archive manifest, failure/sweep state, units, and watcher state remain unchanged;
5. no service restart occurs.

If selector acceptance fails after movement, the retained signed `0.1.17` release is the exact rollback target. The rollback and repeated acceptance must be part of the bounded operator transaction; this readiness audit grants neither install nor rollback authority.

The next independent gate remains exactly:

```text
INSTALL CLOUDX CLOUD 0.1.18
```
