# Cloudx 0.1.19 Local Activation

Date: 2026-07-19

## Decision And Boundary

Cloud `0.1.19` had already passed its independent transaction. The standing operator authorization accepted the local exact gate:

```text
INSTALL CLOUDX LOCAL 0.1.19
```

This authorized signed local staging, selector/entrypoint activation, and shell-source reconciliation only. It did not authorize native-profile reseeding, CPA restart or policy/watcher activation, credential/archive mutation, cloud mutation, Phi mutation, or legacy retirement.

## Baseline And Recovery

- starting selectors `0.1.18/0.1.17`; target release absent
- immutable source `51c5294ed6dd5b504ef4384e9860c70e2593ae78`
- local artifact SHA-256 `2d54f0e8d5b1e38bd4d5e231620c31691b8f5d926c2222ab5be59fc0832dfb03`
- manifest SHA-256 `d6de8428c97ac30371626a6b4f9b41b47da134a5dd3e3900c5d2c90eb3ec996f`
- complete native auth/config, `.zshrc`, signed shell source, CPA binary/config/LaunchAgent, 34-file JSON auth tree, CPA PID `61859`, and eight existing Codex PIDs captured after a real baseline canary
- private recovery job `20260719T073812Z-336c544b` prepared before staging
- recovery snapshots include native auth/config and shell files under mode `0600`; `recover.sh` is mode `0700`, restores exact `0.1.18/0.1.17` selectors and snapshots, checks CPA health, and runs a real official-Codex-through-CPA recovery canary
- recovery `--check` passed before selector movement

## Installation And Acceptance

The bounded transaction cloned immutable `v0.1.19`, verified the exact source, and invoked the tagged canonical installer with the exact confirmation. Because the native auth/config pair was complete, the signed installer selected no seed account. It returned local stage `staged`, activation `active`, and `nativeProfileChanged=false`.

- `current=/Users/hirohi/.local/lib/cloudx/releases/0.1.19`
- `previous=/Users/hirohi/.local/lib/cloudx/releases/0.1.18`
- artifact and manifest match the published SHA-256 values
- self-check reports local `0.1.19`, protocol `1..1`, status `ok`
- `codexx`, `cloud`, and `cloudx-update` resolve through the signed current artifact
- fresh-shell `codexx api` preserves `/opt/homebrew/bin/codex` and selects the existing API profile
- native auth/config, CPA binary/config/LaunchAgent, and the 34-file CPA JSON tree are byte-identical
- CPA remained PID `61859`; no service restarted
- all eight pre-existing Codex PIDs survived
- real official-Codex-through-CPA post-install canary passed
- cloud remained `0.1.19/0.1.18` with CPA PID `1613475`, restart count `0`

## Next Gate

Both endpoints now run signed `0.1.19` with signed `0.1.18` retained. The rollback-bounded M4B production acceptance may now be repeated. Local `.policy.3` activation remains deferred until the independent five-sample zero-connection gate passes; current established traffic is not terminated.
