# Cloudx 0.1.10 Native CPA Activation

## Release Recovery And Publication

Immutable `0.1.9` remained failed-release evidence after its cloud artifact rejected candidate staging with a stale packaged trust root. Signed stable was restored to `0.1.8`, no cloud `0.1.9` release directory was created, and neither endpoint activated that version.

Cloudx `0.1.10` fixed the cloud trust-root mirror and added a regression requiring repository, local, and cloud signer files to be byte-identical.

- source commit: `d495808539c18c2acb416c73d048844a9935bcd6`
- release-key fingerprint: `SHA256:jJAS0rbecTDcU4QD3JjkJFv/5YpFyAOAtvdaf0fY4j0`
- immutable artifact ref: `release-artifacts/v0.1.10`
- artifact ref SHA: `af99c29225f3aac21f50ce3f522749c4e6348835`
- signed stable ref SHA: `14aca5900dc464da268c567f5c0690b4f873e611`
- local artifact SHA-256: `75fc8d18aa47c99bbaf8b0500702a2ae3c73008d976054c5cbfed68c08c8cd3a`
- cloud artifact SHA-256: `bc77149ecc27ac17cc64fd449944389819a6d860a970dd9f9dc810ec6f135502`
- manifest SHA-256: `cb25cdd89157b8670d11dfc8ed0db6dfd57e8716c96368a8c1a4df7d924cd1f5`
- manifest signature SHA-256: `31349f12a0b02f74872f2c09f9e0906b8376878943810c5f5f7ed492b5c01db5`
- offline bundle SHA-256: `1fe5700bae94dbb34cf09dec0b52dd75b55905caa3760070082acfeb7f70f8c1`
- stable index SHA-256: `d607bc9c326e763e0c31f9f728fbfa6ca1a981b21b359aed65c2453686316f5c`
- stable signature SHA-256: `ff01ce221c039c6b8099691052a0ad302ca5f719d1a25e638c3d74813eb64e5d`
- private offline archive: `/Users/hirohi/.local/state/cloudx-release/archives/0.1.10`

The private release key remained outside Git, release directories, offline archives, and the cloud endpoint. Fresh clones of both published refs repeated signature, artifact, source-commit, stable-index, and manifest-digest verification. Before publication, both candidate artifacts completed real isolated stage operations using their own embedded trust root.

## Staging, Activation, And Rollback

Candidate-verified staging completed idempotently on both endpoints without changing active links. Cloud activated first and local second. Final release state is:

- local current: signed `0.1.10`
- local previous: signed `0.1.8`
- cloud current: signed `0.1.10`
- cloud previous: signed `0.1.8`

The endpoint-only rollback rehearsal completed:

1. local `0.1.10 -> 0.1.8 -> 0.1.10` while cloud remained `0.1.10`
2. cloud `0.1.10 -> 0.1.8 -> 0.1.10` while local remained `0.1.10`

At each mixed-version point, handshake protocol 1, official Codex resolution, local API selection, and cloud gateway HTTP `200` remained valid. Complete post-activation official-Codex requests returned `CLOUDX_0110_API_OK` through the retained local CPA and `CLOUDX_0110_CLOUD_OK` through the isolated Cloudx broker.

## Native Unit Transaction

The signed `0.1.10` service and timer passed `systemd-analyze verify`. The service contains no condition, environment variable, or read-only path for `/opt/codex-gateway`.

- installed service SHA-256: `cd98364d35b940ca165a553b4d80474c7bc7adee0138f87f42990553d68f1ea6`
- installed timer SHA-256: `14b8058c445b063ed2d89b1fa4ff3d71cad5b06d4b13c6e8dd88e3fd7d4abbe0`
- rollback root: `/var/lib/cloudx/cpa-health-service-backups/20260715T130117Z-0.1.10`

The rollback root is mode `0700`; its unit files, prior state archive, systemd evidence, journal evidence, inventory, and hashes are mode `0600`. It retains the former `0.1.8` compatibility service at SHA-256 `eec8b21fcdba6e0f6137f841762eae5232b0d445ea00ee5a9d4e43a4831b5cc4`.

The transaction atomically replaced only `cloudx-cpa-health.service` and `.timer`, performed one `daemon-reload`, and restarted only the timer. The failure trap would restore both former units, reload systemd, and restart the same timer. No service was manually started by the transaction.

## Natural Observation

Two distinct natural systemd invocations completed with status 0:

- first InvocationID: `6355984bdaa44fe0a9d0d67f747f3ecf`
- second InvocationID: `534e794be090499b89d78ed8609969e3`
- state: `healthy`
- total: `15`
- ready: `10`
- limited: `5`
- failed: `0`
- archived: `0`
- pending archive candidates: `0`

Before and after observation, auth remained 15 files with anonymous inventory digest `ed7b6c095c09f9a20dc42210673fb4d9d11fda2c980ea042d95aeae7e5d0838c`; archive remained empty with digest `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`. State files remain mode `0600` under a mode-`0700` directory. Journal output contains aggregate fields only.

Formal `cloudx.health.v1` reports producer `0.1.10`, healthy gateway, ready importer, fresh state, 10 available, and 5 limited accounts. Phi's restricted consumer successfully reads producer `0.1.10`; it truthfully reports degraded capacity because those five accounts are limited, not because the gateway or import dependency failed.

## Continuity And Remaining Dependency

The following identities retained their original PIDs/start times and zero restart counts where applicable:

- local CLIProxyAPI: PID `38189` on `127.0.0.1:8317`
- cloud CLIProxyAPI: PID `977036`, restart count `0`
- old cloud importer: PID `133756`, restart count `0`
- Tailscale: PID `32776`
- mihomo: PID `277808`
- sshd listener: PID `916060`
- five pre-existing local Codex processes

The private `codexx-legacy` command remains healthy. The Cloudx broker has zero leases and retains its configured one-hour idle lifecycle; it does not own the legacy local CPA listener.

Cloudx CPA health no longer imports `/opt/codex-gateway/codexx_app`. The active legacy HTTP importer still executes `/opt/codex-gateway/cloud_import_api.py`, which imports `codexx_app.cloud_import_server`; Phi rollback policy also retains a historical provider-monitor path under that runtime. The directory is therefore not retired by this activation and remains gated on separate importer and rollback dependency work.
