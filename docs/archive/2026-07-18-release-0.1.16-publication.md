# Cloudx 0.1.16 Signed Publication

Date: 2026-07-18

## Decision And Boundary

The operator supplied the exact confirmation:

```text
PUBLISH CLOUDX 0.1.16 SIGNED RELEASE WITHOUT ACTIVATION
```

This authorized immutable tag and signed release publication only. It did not authorize real endpoint staging or activation, shell/profile installation, unit changes, service restart, CPA activation, credential mutation, archive movement, or legacy removal.

## Final Preflight

- worktree: clean
- repository `VERSION`: `0.1.16`
- local `HEAD` and `origin/main`: `ec77369a990418f2a990874d1d7bd4b9d2c7fe04`
- main CI run: `29637937166`, successful on the same HEAD across Ubuntu/macOS and Python 3.9/3.12
- signer fingerprint: `SHA256:oEhvhqj9U4wM8zLz8w43A/fvMN+BRNXO1k5/3eVPh9o`
- `v0.1.16`: absent before publication
- `release-artifacts/v0.1.16`: absent before publication
- previous stable ref: `78fe78303f9d57c592b103e11c0fdca1c373b37c`
- local CPA: PID `38189`, baseline binary selected
- cloud CPA: PID `977036`, restart count `0`, baseline binary selected

The CI failure on the preceding commit was traced to shallow checkout omitting annotated release history required by published bridge evidence. A fresh shallow clone reproduced the failure; fetching complete tag history made the same checkout pass. Source `ec77369...` sets CI `fetch-depth: 0`, retains a regression for that contract, and passed run `29637937166` before publication.

## Immutable Publication

Annotated tag `v0.1.16` was created once and pushed once. It resolves as:

```text
tag object=6d5425dccb4e0a6588ff29ceabdf7f5f15991457
source commit=ec77369a990418f2a990874d1d7bd4b9d2c7fe04
```

Tag-triggered workflow `29640659405` started at `2026-07-18T10:19:39Z` and concluded successfully. The release job passed:

- repository `./verify.sh`
- exact release-tag verification
- signing-key load
- signed local/cloud release build
- manifest/source/signature/stable evidence verification
- immutable signed-ref publication
- GitHub Release asset publication

The published identities are:

```text
artifact ref commit=9513ff87b3b2e45d2b3609f0746248a7422d34b2
stable ref commit=bba9f619fc2d3e57cbd1b2808fe97ac58e805aef
source commit=ec77369a990418f2a990874d1d7bd4b9d2c7fe04
signer fingerprint=SHA256:oEhvhqj9U4wM8zLz8w43A/fvMN+BRNXO1k5/3eVPh9o
manifest SHA-256=9ac48648b9e00bc0fcdbc33517cb3f97bb545211829626e6423281549f7b4fee
```

The GitHub Release is latest, neither draft nor prerelease, and contains exactly:

- `SIGNING_IDENTITY`
- `allowed_signers`
- `cloudx-local-0.1.16.pyz`
- `cloudx-cloud-0.1.16.pyz`
- `cloudx-0.1.16-offline.tar.gz`
- `manifest.json`
- `manifest.json.sig`

Artifact hashes are:

```text
cloudx-local-0.1.16.pyz  b8fb5d72192d140b212fcc3aacef05066972bdfb720a83b507b069b47920c323
cloudx-cloud-0.1.16.pyz  b6257a5bbe128f159019ea0592be6eb3f3f5e38d5e109bdb293af06230ed8cfe
cloudx-0.1.16-offline.tar.gz  7679bfd25a308d3390cd2476492ecadb97cdc4dab19bc85a97b534c711e4919f
```

## Independent Fresh Verification

Fresh shallow clones of `release-artifacts/v0.1.16` and `release/stable`, plus fresh downloads of all seven GitHub Release assets, independently proved:

1. the current release root accepts the manifest signature;
2. the previous release root rejects the same manifest signature;
3. stable selects `refs/heads/release-artifacts/v0.1.16` and its manifest digest matches;
4. every non-bundle GitHub asset is byte-identical to the immutable artifact ref;
5. both downloaded artifacts report `cloudx.self-check.v1`, version `0.1.16`, status `ok`;
6. the downloaded local artifact accepted stable and staged the offline bundle as `staged`, then `already-staged`;
7. the downloaded cloud artifact staged the same bundle as `staged`, then `already-staged`;
8. both isolated roots remained without `current` or `previous` selectors.

## Runtime Truth And Next Gate

No real local or cloud Cloudx release directory, selector, shell hook, profile, CPA launcher/unit, CPA process, gateway/importer process, listener, Phi credential, health publisher, account, or archive was changed by publication or independent verification.

Signed `0.1.16` now satisfies the M4B publication gate. Real endpoint installation remains separately confirmed in cloud-first then local order. CPA activation remains a later distinct decision, and local CPA activation must use the deferred communication-safe worker rather than synchronously restarting the service from the CPA-backed authorizing turn.

Repository development advances immediately to `0.1.17` so no later source change can produce a second artifact under the immutable `0.1.16` identity. The deferred local CPA scheduler remains pinned to required active signed release `0.1.16`, not the mutable development version.

Post-publication closeout then passed architecture validation, 360 tests, truthful blocked cross-repository gates, and healthy local/cloud `0.1.17` development builds without changing a real endpoint or service.
