# Cloudx 0.1.20 Cloud Activation

Date: 2026-07-19

## Decision And Boundary

The standing operator authorization accepted the independent exact gate:

```text
INSTALL CLOUDX CLOUD 0.1.20
```

This authorized signed cloud staging and selector activation only. It did not authorize local installation, CPA/importer restart, watcher/unit changes, credential mutation, policy activation, Phi mutation, or legacy retirement.

## Baseline And Recovery

- immutable source `eefd8a4aa70d554ac32babe3ff7aa1ae9996e875`
- artifact ref `d6bce9346d86b2b133057df01ced068ef3eef9b2`
- cloud artifact SHA-256 `801a5977362cac6653f18c83efc9bd414126feebc0297cebc35e8c4bd514216e`
- manifest SHA-256 `9a9984cf18de1797900ea3710c463abcca20a1958a4809627065f73c3d7e23c7`
- starting selectors `0.1.19/0.1.18`; target release absent
- CPA PID `1613475`, importer PID `133756`, both active/running with restart count `0`
- one active credential, 45 archive entries, zero failure receipts, absent trigger
- both watcher paths, nine unit/drop-in digests, two private prerequisite digests, and local communication captured before mutation

Root-only transaction `20260719T082934Z-8f5ce554` stored mode-`0700` `recover.sh` and mode-`0600` `RECOVERY.txt` before staging. Its exact command restores selectors to `0.1.19/0.1.18`, verifies signed self-check/release status, and requires CPA/importer active without restarting either service. Recovery `--check` passed before activation.

The deploy bundle SHA-256 is `d7e7e4fff2bad1cdae979aa8553bc0899d6f6211771b1e1d7d7e5dce39767a58`; it contains only annotated `v0.1.20` and `release-artifacts/v0.1.20` with complete history.

## Installation And Acceptance

The transaction cloned the immutable tag from the offline bundle and invoked its canonical installer with the exact confirmation. The installer fetched the signed artifact from the same bundle and returned stage `staged`, activation `active`, and `serviceRestarted=false`.

- `/opt/cloudx/current -> /opt/cloudx/releases/0.1.20`
- `/opt/cloudx/previous -> /opt/cloudx/releases/0.1.19`
- installed artifact and manifest match the published SHA-256 values
- self-check, release status, and handshake report `0.1.20`, protocol `1..1`, status `ok`
- CPA/importer state, credential/archive/failure/sweep aggregates, watcher states, all captured unit/drop-in bytes, and private prerequisite bytes match the baseline
- real official-Codex-through-local-CPA traffic passed after cloud installation
- no service restarted and no credential moved

## Next Gate

Cloud-first ordering is satisfied. Local signed `0.1.20` installation remains a separate transaction with native-profile and shell byte preservation, prebuilt selector recovery, every live CPA/Codex identity retained, and real communication canaries. CPA `.policy.4` staging remains later and process-neutral.
