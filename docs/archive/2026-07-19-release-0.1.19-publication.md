# Cloudx 0.1.19 Signed Publication

Date: 2026-07-19

## Decision And Boundary

The standing operator authorization supplied the exact independent gate:

```text
PUBLISH CLOUDX 0.1.19 SIGNED RELEASE WITHOUT ACTIVATION
```

This authorized immutable tag and signed release publication only. It did not install or activate either endpoint, move a production selector, install a shell hook or profile, change a unit, restart a service, mutate a credential/archive, activate a CPA policy/watcher, change Phi, or retire a legacy path.

## Final Preflight

- clean worktree and pushed `main`
- repository `VERSION`: `0.1.19`
- local `HEAD` and `origin/main`: `51c5294ed6dd5b504ef4384e9860c70e2593ae78`
- main CI run `29669596380`: successful on the complete matrix
- non-publishing signing canary `29669662202`: successful on the same source; both publication steps skipped
- `v0.1.19`, `release-artifacts/v0.1.19`, and the GitHub Release: absent before publication
- previous stable ref: `d3275fd150bceaa50c216c13a99969b58409992d`

## Immutable Publication

Annotated tag `v0.1.19` was created once and pushed once:

```text
tag object=205f25b2f3ad060b66082716a69ef2e4006c18ea
source commit=51c5294ed6dd5b504ef4384e9860c70e2593ae78
```

Tag workflow `29677581155` completed successfully. Repository verification, exact tag verification, signing-key load, signed local/cloud build, release-evidence verification, signed-ref publication, and GitHub Release publication all passed.

Published identities:

```text
artifact ref=d4bddf35a1305186ffe568438ceaca6afb5cce61
stable ref=e5f7fafb7539b09f5b3e0f5424999bccd5a06dac
manifest SHA-256=d6de8428c97ac30371626a6b4f9b41b47da134a5dd3e3900c5d2c90eb3ec996f
local SHA-256=2d54f0e8d5b1e38bd4d5e231620c31691b8f5d926c2222ab5be59fc0832dfb03
cloud SHA-256=33b575ec67844f36c2343cbef255cf740a93d288d910cf0405fdab1bb299e430
offline bundle SHA-256=cce684675b289e6fac34a1f56e82bb9be5624b7445a671e8b111d1b6689cf3d6
```

GitHub Release `356279554` is non-draft and non-prerelease and contains exactly seven assets: both artifacts, offline bundle, manifest, signature, signing identity, and allowed signers.

## Independent Verification

Fresh clones of the immutable artifact and stable refs plus fresh downloads of all seven assets proved:

1. the current signer accepts the manifest and the pre-rotation signer rejects it;
2. the manifest binds version `0.1.19` to source `51c5294...` and forbids automatic activation;
3. stable selects `refs/heads/release-artifacts/v0.1.19` and its manifest digest matches;
4. all non-bundle Release assets are byte-identical to the immutable artifact ref;
5. every offline-bundle member is byte-identical to the artifact ref;
6. local and cloud self-checks return version `0.1.19`, protocol `1..1`, status `ok`;
7. isolated local and cloud staging each return `staged`, then `already-staged`;
8. neither isolated root gains `current` or `previous` selectors.

The first independent verification command stopped after the signature/assets checks because it expected the old generic `status` field instead of the local stage document's `local` field. The corrected retry repeated the fresh downloads and all checks above. Neither command accessed or changed a real endpoint.

## Runtime Truth And Next Gate

After publication, local still selects signed Cloudx `0.1.18/0.1.17`. The external local CPA remains PID `61859` on port `8317`; 114 established socket rows and nine Codex processes were observed without termination or restart.

Cloud still selects signed Cloudx `0.1.18/0.1.17`. `cliproxy.service` remains active/running as PID `1613475` with restart count `0`; `codex-import.service` remains PID `133756` with restart count `0`; the active pool retains one useful credential, the reversible archive retains 45 prior entries, and failure/trigger inputs are empty.

Publication and verification changed no real release directory, selector, shell hook, profile, launcher, unit, process, listener, credential, archive, watcher, CPA policy, or Phi state.

Signed `0.1.19` carries the corrected non-refreshable-401 archive vocabulary and matching local policy/watcher gates. Real endpoint installation remains separately confirmed in cloud-first then local order with signed `0.1.18` retained. M4B production acceptance is repeated only after the matching signed consumer is active.
