# Cloudx 0.1.21 Cloud Activation

Date: 2026-07-19

## Decision And Boundary

The continuing Roadmap authorization accepted the independent cloud-first installation gate:

```text
INSTALL CLOUDX CLOUD 0.1.21
```

This authorized signed cloud staging and selector activation only. It did not authorize local installation, CPA/importer restart, watcher/unit changes, credential mutation, CPA policy staging or activation, Phi mutation, or legacy retirement.

## Baseline And Recovery

- immutable source `f46a0e39887722bd20301270dc3e84d030f8058d`
- artifact ref `00148d962a8e90b213b36ef5cb8fcae46a046029`
- cloud artifact SHA-256 `0e96deebc121d5a331fc9e9ceb0621ccb330b613940050c95eb48d6da483ccc7`
- manifest SHA-256 `fe6a1086fe1a878a3e73a0485a72c5751b606ce382e4c5da0d4d1599b8fac119`
- starting selectors `0.1.20/0.1.19`; target release absent
- CPA PID `1693505`, importer PID `133756`, both active/running with restart count `0`
- one top-level active credential, 46 top-level archive JSON files, zero failure receipts, absent trigger
- all protected credential/archive/failure/sweep tree aggregates, signed watcher states, CPA unit/drop-in bytes, and private prerequisite digests captured before mutation
- six local Codex PIDs captured after a real baseline official-Codex-through-CPA canary

Root-only transaction `20260719T094620Z-037d1c5c` stored the immutable deploy bundle, mode-`0700` transaction/apply/recovery tools, mode-`0600` baseline and manual, and an exact automatic recovery trap before staging. Recovery `--check` verified its own bytes, the complete bundle, signed `0.1.20/0.1.19`, exact starting selectors, and both external services without restarting either one.

The deploy bundle SHA-256 is `68160064a4a91dd49a1a402fbfc6c49290386abaf897b1357c2ee351760ba5a1`. It contains only annotated `v0.1.21` and `release-artifacts/v0.1.21`, with complete history.

Manual recovery remains:

```text
sudo /var/lib/cloudx/install-transactions/20260719T094620Z-037d1c5c/recover.sh 'RECOVER CLOUDX CLOUD 0.1.21 TO 0.1.20'
```

It restores selectors to `0.1.20/0.1.19`, verifies both signed rollback releases and service availability, and does not restart CPA/importer, mutate credentials, or remove the retained `0.1.21` release.

## Installation And Acceptance

The transaction cloned the immutable tag from the offline bundle, verified source/tag identities and the non-authorizing installer plan, then invoked the tagged canonical installer with the exact confirmation. The installer fetched the signed artifact from the same bundle and returned stage `staged`, activation `active`, and `serviceRestarted=false`.

- `/opt/cloudx/current -> /opt/cloudx/releases/0.1.21`
- `/opt/cloudx/previous -> /opt/cloudx/releases/0.1.20`
- installed artifact and manifest match the published SHA-256 values
- self-check, release status, and handshake report `0.1.21`, protocol `1..1`, status `ok`
- CPA/importer identities and restart counts, all protected runtime tree aggregates, watcher states, unit/drop-in bytes, and private prerequisite bytes match the baseline exactly
- all six captured local Codex PIDs survived
- real official-Codex-through-local-CPA traffic passed before and after cloud installation
- no service restarted and no credential moved

## Next Gate

Cloud-first ordering is satisfied. Local signed `0.1.21` installation remains a separate transaction with native-profile and shell byte preservation, prebuilt selector/profile recovery, every live CPA/Codex identity retained, and real communication canaries. CPA `.policy.5` staging remains later and process-neutral.
