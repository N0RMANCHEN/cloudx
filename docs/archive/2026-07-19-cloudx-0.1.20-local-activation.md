# Cloudx 0.1.20 Local Activation

Date: 2026-07-19

## Decision And Boundary

Cloud `0.1.20` had already passed its independent transaction. The standing operator authorization accepted:

```text
INSTALL CLOUDX LOCAL 0.1.20
```

This authorized signed local staging, selector/entrypoint activation, and shell-source reconciliation only. It did not authorize CPA restart, policy/watcher activation, credential mutation, cloud mutation, Phi mutation, or legacy retirement.

## Baseline And Recovery

- starting selectors `0.1.19/0.1.18`; target release absent
- immutable source `eefd8a4aa70d554ac32babe3ff7aa1ae9996e875`
- local artifact SHA-256 `bce19e8b27f35bcb95637a5a3afa82c01feb51765763d1b3f34f14dad0e315fd`
- manifest SHA-256 `9a9984cf18de1797900ea3710c463abcca20a1958a4809627065f73c3d7e23c7`
- exact native auth/config, `.zshrc`, signed shell source, CPA binary/config/LaunchAgent, 37-file auth JSON aggregate, CPA PID `61859`, and six existing Codex PIDs captured after a real baseline canary
- private recovery job `20260719T083822Z-8a0b219c` prepared before staging
- recovery snapshots and job files are mode `0600`; `recover.sh` is mode `0700`, restores exact `0.1.19/0.1.18`, restores native/shell snapshots, verifies signed self-check and CPA health, and runs a real recovery canary
- recovery syntax, snapshot/tool/bundle digests, source, selectors, signed rollback command entry, self-check, and CPA health passed through `--check`

## Installation And Acceptance

The bounded transaction invoked the immutable tag's canonical installer and fetched the signed artifact from the same audited offline bundle used by cloud. It returned stage `staged`, activation `active`, and `nativeProfileChanged=false`.

- `current=/Users/hirohi/.local/lib/cloudx/releases/0.1.20`
- `previous=/Users/hirohi/.local/lib/cloudx/releases/0.1.19`
- artifact and manifest match the published SHA-256 values
- self-check reports local `0.1.20`, protocol `1..1`, status `ok`
- `codexx`, `cloud`, and `cloudx-update` resolve through signed current
- fresh-shell `codexx api` resolves `/opt/homebrew/bin/codex`, the existing API profile, and mode `api`
- native auth/config, `.zshrc`, shell source, CPA binary/config/LaunchAgent, and the 37-file auth aggregate are byte-identical to baseline
- CPA remained PID `61859`; all six captured Codex PIDs survived
- real official-Codex-through-CPA acceptance traffic passed
- cloud remained `0.1.20/0.1.19` with CPA PID `1613475`, importer PID `133756`, restart counts `0`, one active credential, 45 archive entries, and empty failure/trigger inputs
- no service restarted

## Next Gate

Both endpoints now run signed Cloudx `0.1.20` with signed `0.1.19` retained. Exact `.policy.4` staging is process-neutral and may proceed cloud then local through separate confirmations. Activation remains separate; local activation is prohibited until five consecutive zero-connection samples pass, and no Codex process may be terminated to manufacture that state.
