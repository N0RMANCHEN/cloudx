# Cloudx 0.1.12 Signed Release And Staging

## Release Identity

Cloudx `0.1.12` carries the local shell hardening that removes regenerated codex-plus account `.local/bin` entries from an inherited legacy `PATH` while retaining official Codex, official Git, account selection, Codex temporary paths, and private recovery.

- source commit: `1f94816d7effc62bb3199158066c1ed0b6ef38a6`
- release-key fingerprint: `SHA256:jJAS0rbecTDcU4QD3JjkJFv/5YpFyAOAtvdaf0fY4j0`
- immutable artifact ref: `release-artifacts/v0.1.12`
- artifact ref SHA: `6378031a2e8f93534484b61f8e07ba86d7e82c34`
- signed stable ref SHA: `61d61f49c5ecc471ed81d1ee71a9e6f871aa7c87`
- local artifact SHA-256: `10065be877ab788ca3a9b7cd1ebb5013369989d98007b3dea31472d0035fec28`
- cloud artifact SHA-256: `e6ecb020465d2c681ecee4580b5f55258f1802b7247792940363460e8107d6ba`
- manifest SHA-256: `9f4c6e59db3975756aad3ae048087beb54eada1ba2120be1d8c24482e5976e86`
- manifest signature SHA-256: `c6c2d2f1bdaf70caa157593b225fe173f86a43e433ee5454d6f229cfb10f2fa3`
- offline bundle SHA-256: `e4550c7d04042237fe778d15fd7991776f0b56b8f5bd7a0f4d19b1cb9c06b02d`
- stable index SHA-256: `7fa43ddbf6177380de2590b135c33aa7fe163f4e62850c37489093546a7e4034`
- stable signature SHA-256: `6df0410d95ca8acc99d1aec45d0eacf99ebe3f593041b46ca0d127fc5c8222cb`
- packaged shell hook SHA-256: `e76347b0589887000022fb9d1562bd8f722f200a0baec30bdc24ca3011771671`
- private offline archive: `/Users/hirohi/.local/state/cloudx-release/archives/0.1.12`

The private key remained mode `0600` outside Git, release directories, offline bundles, and both endpoints. The private archive is mode `0700`; all retained files are mode `0600`.

## Candidate Verification

The exact release source passed architecture validation, 120 tests, and deterministic local/cloud artifact builds. The signed manifest and stable index verified against the current trust root.

Real isolated staging used the signed offline bundle and each candidate artifact's embedded trust root. Both endpoints returned `staged` on the first attempt and `already-staged` on repetition. Neither isolated root created a `current` link. The staged local artifact contained the exact hardened hook hash above.

Fresh clones of both published refs repeated:

- manifest signature and artifact verification
- stable-index signature and manifest-digest verification
- source commit, version, and immutable artifact-ref verification
- exact local, cloud, manifest, and signature hashes

## Endpoint Staging

The installed signed `0.1.11` updater first reported `current=0.1.11`, `available=0.1.12`, and `updateAvailable=true`. A normal dual-endpoint stage from immutable ref `release-artifacts/v0.1.12` returned:

```text
local  staged -> already-staged
cloud  staged -> already-staged
activated false
```

Final endpoint selection remains:

- local current: signed `0.1.11`
- local previous: signed `0.1.10`
- cloud current: signed `0.1.11`
- cloud previous: signed `0.1.10`

The staged local and cloud artifact hashes match the signed manifest exactly. The cloud artifact returned `cloudx.self-check.v1`, component `cloud`, version `0.1.12`, protocol 1, and status `ok`.

## Continuity

Staging preserved these active identities and zero restart counts where applicable:

- codex-plus control server: PID `729`
- external local CPA: PID `38189` on `127.0.0.1:8317`
- five codex-plus execution parents and their five official Codex children
- cloud CLIProxyAPI: PID `977036`, restart count `0`
- old cloud HTTP importer: PID `133756`, restart count `0`

No shell source, account record, regenerated Git shim, service unit, endpoint link, listener, credential, auth file, or rollback archive was changed by staging.

## Remaining Activation Gate

Signed `0.1.12` is staged but inactive. Activating cloud first and local second, installing the new local shell source, validating a fresh legacy-environment reload, and rehearsing endpoint-only N-1 rollback require a separate explicit operator decision. The active codex-plus sessions and local CPA remain protected until that gate is accepted.
