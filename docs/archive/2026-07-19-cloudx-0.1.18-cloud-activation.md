# Cloudx 0.1.18 Cloud Activation

Date: 2026-07-19

## Decision And Boundary

The operator explicitly approved every remaining independent Roadmap confirmation through M5 while requiring local communication continuity. This included the distinct cloud installation gate, and the bounded transaction supplied the canonical exact confirmation:

```text
INSTALL CLOUDX CLOUD 0.1.18
```

This transaction authorized signed Cloudx `0.1.18` cloud staging and selector activation only. It did not authorize local installation, CPA restart, watcher activation, credential import/archive/restore, unit changes, Phi mutation, or legacy retirement.

## Baseline

- immutable source: `f94c926daa929d8d9801722013408eb9ebb7e90a`
- immutable artifact ref: `5e345ed8cbf0373ac9e6214e45358187a524ea5d`
- manifest SHA-256: `b5ab79e932f215730452dd6aa8afabe629a23118a489ec5c03054d5e0856ee23`
- cloud artifact SHA-256: `1cba50617522634c09584d7cc9d1a800df056256c0201fd325cb54afa8ca1426`
- starting selectors: `current=0.1.17`, `previous=0.1.16`
- target release directory absent
- CPA PID `1613475`, importer PID `133756`, both active/running with restart count `0`
- zero active auth JSON files, 45 private reversible archive entries, zero failure receipts, zero sweep triggers
- failure/sweep watcher path units absent/inactive
- scoped credential and environment prerequisites regular, private, and unchanged
- local Cloudx `0.1.17/0.1.13`, CPA PID `61859`, health `ok`

## Bounded Attempts

The first operator wrapper stopped before installer invocation because its temporary-tag `git rev-parse` check omitted the clone working directory. Read-only inspection proved `/opt/cloudx/releases/0.1.18` remained absent, selectors stayed `0.1.17/0.1.16`, and every service/runtime baseline remained unchanged. No rollback or endpoint mutation had actually occurred.

The corrected independent retry cloned immutable tag `v0.1.18` through explicit `HTTP_PROXY`/`HTTPS_PROXY=http://127.0.0.1:7890` and Git HTTP/1.1. It verified the source commit before invoking the immutable installer with the exact confirmation. The installer fetched only `release-artifacts/v0.1.18`, returned stage `staged`, activation `active`, and `serviceRestarted=false`.

The transaction prepared signed `0.1.17` selector rollback before movement and would have invoked it on any failed post-activation assertion. No rollback was needed on the successful retry.

## Acceptance

- `/opt/cloudx/current -> /opt/cloudx/releases/0.1.18`
- `/opt/cloudx/previous -> /opt/cloudx/releases/0.1.17`
- target release is root-owned and contains the exact published manifest/artifact bytes
- self-check reports cloud `0.1.18`, protocol `1..1`, status `ok`
- release status reports `current=0.1.18`, `previous=0.1.17`, and the exact artifact digest
- handshake reports product version `0.1.18` and protocol `1..1`
- CPA remained PID `1613475`, active/running, restart count `0`, health `ok`
- importer remained PID `133756`, active/running, restart count `0`
- credential/environment bytes, active auth, 45-entry archive, failure/sweep inputs, systemd unit bytes, and watcher states remained unchanged
- no service restarted
- local Cloudx remained `0.1.17/0.1.13`; local CPA remained PID `61859`, healthy, with the live communication path intact

## Next Gate

Cloud-first ordering is now satisfied. The separately approved local transaction is next and must use the signed no-reseed path recorded in the local readiness evidence, with private rollback prepared before selector movement and a real official-Codex-through-CPA canary after activation:

```text
INSTALL CLOUDX LOCAL 0.1.18
```
